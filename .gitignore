# Secrets — NEVER commit
.env
*.pem
*_private*.key
kalshi_private*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Scratch / generated chart intermediates
data/scratch/
*.tmp
.DS_Store

# SQLite analysis mirror — derived from the committed JSON, rebuilt each run
data/*.db
*.sqlite
*.sqlite3
data/history/markets.jsonl
data/history/_harvest.log
data/history/_smoke.jsonl
data/history/_harvested_series.json
